
#### 第一、二、三 题是非常常规的基础题目；
第一题按照平方公式展开，$i^2$ 换为-1即可解出答案；
第二题解 $\sqrt x=x$ 这个方程应该也是人人都会的；
第三题两边同时平方的方法也是平时用的很多的套路；两个式子两边同时平方展开，上下相减，即可得出答案；


#### 第四题为基础的双曲线求解，
过点（1,0）带入解出a方；再带入$a^2$和另一个点解出$b^2$；最后带入渐近线方程，搞定；

#### 第五题考了棱台体积，
今年应该问题不大了，毕竟都做过去往年一卷、二卷的高考真题；

#### 第六题考察排列组合，要把题目理解清楚才能做；
它的要求有两个，甲乙同组、丙丁不同组，那么分类情况就很好确定了，甲乙同在A或者B，分成两类，丙丁分开仍在A和B，又是两类；最后分类相加即可；
现在，若甲乙在A组，丙也在A组，则丁一定是在B组，A组还剩一个位置，从剩余的4个人中选一个人放到这个位置即可；可能有同学会疑惑，A组和B组里面的人还能再来一次全排列的呀，他们的不同顺序代表不同的排序结果，那这里就要注意了，题目的要求是把8个人平分两组，唯一的要求是甲乙在一起，丙丁不在一起；在这样的要求下，可以怎么分配；所以组内顺序，题目是不要求的；
我们使用一个$C_4^1$就够了，丁在A丙在B也是一样的，空下的一个位置在剩余的4个人中找一个来放上即可；

如果甲乙同时在B组也是同样的道理；

#### 第七题考察 倍角公式，三角函数化简求职；
因为$\cos2a$展开的选择较多，我们先把sin2a展开化简；
因为 2sina不等于0,两边约分化简一下；化简之后，只剩cos，那么cos2a的展开方向也就清晰了，展开为$2\cos^2a-1$的形式;这样等好两边都是$\cos^2 \alpha$,这样就可以把$\cos^2 \alpha$的数值算出来，然后根据$\sin^2 \alpha + \cos^2=1$计算出$\sin^2 \alpha$的数值；确定sin a和cos a的正负之后就可以开方算出他们的数值；
a在第二相限，sin为正、cos为负，解出数值带入计算，搞定！

#### 第八题考察抽象函数的性质；

解法必须靠性质推导，通过[3/2,3]这个局部区间解决问题；抽象函数的题多练一下，那么这道题其实是还挺符合我们平时做的练习的；

局部区间，大概率是需要通过周期求解的，函数周期，f(x)=f(x+T),所以先把已知条件的  减2 处理一下；

令x为x-2，则可以得到f(x+2)=-f(x),现在还不是我们需要的f(x)=f(x+T)，
再套一次 ——再令x=x+2,就可以得到f(x+4) = f((x+2)+2) = -f(x+2) = -(-f(x)) = f(x)

周期T等于4就可以推断出来了，   

由周期=4,我们可以推断出f(3/2)=$f(-\frac{5}{2})$=f(5/2)
$\frac{3}{2}$、$\frac{5}{2}$，都在这个x的取值范围，我们便有了第一个等式；恰好a的数值就解出来了，

我们再继续找出另外的等式计算出b，想起平时凑f(1)数值的情形，令x=1,带入原式，再通过偶函数转换，能很自然的就可以计算出f(1)的数值。不过1不在取值范围，不能直接使用，
通过step1的结果f(x+2)=-f(x),令x=1,可以知道f(3)=-f(1)=0，3在这个取值范围，可以带入使用，之后便能计算出b的数值；

#### 第九题 圆的方程 · 圆与圆的位置关系
题目给出两个圆：单位圆 $\odot O: x^2+y^2=1$ 和含参圆 $\odot A: x^2+y^2-6x-8y+k=0$。核心是配方求出 $\odot A$
的圆心和半径——这一步完成，四个选项就都能判断了。

配方求 ⊙A 的圆心和半径 

$$
\begin{aligned}
\odot A:; &x^2+y^2-6x-8y+k=0 \
\Rightarrow;& (x^2-6x+9) + (y^2-8y+16) = 25-k \
\Rightarrow;& (x-3)^2 + (y-4)^2 = 25-k
\end{aligned}
$$

圆心 $A(3,4)$，半径 $r = \sqrt{25-k}$（要求 $k < 25$ 才为实圆）。

$\odot O$：圆心 $O(0,0)$，半径 $r_O=1$。

##### B 选项：k=9 时 ⊙A 与 x 轴相切 

$k=9$ 时，$r = \sqrt{25-9} = 4$。

圆心 $A(3,4)$ 到 $x$ 轴的距离 $= |4| = 4 = r$。距离等于半径 → 相切。

##### C 选项：k=-11 时 ⊙A 和 ⊙O 相切 

$k=-11$ 时，$r_A = \sqrt{25-(-11)} = \sqrt{36} = 6$。

两圆圆心距：$|OA| = \sqrt{3^2+4^2} = 5$。

比较：$r_A - r_O = 6-1 = 5 = |OA|$。

圆心距等于半径差 → 内切。"相切"包含内切和外切，选项成立。

C ✓。

##### D 选项：相交时公共弦所在直线 

 ▎ "两圆相交时，两交点所在直线方程为 6x+8y-k-2=0"

两圆方程直接相减即得根轴（公共弦所在直线）：

$$
\begin{aligned}
(x^2+y^2-6x-8y+k) - (x^2+y^2-1) &= 0 \
-6x-8y+k+1 &= 0 \
6x+8y-k-1 &= 0
\end{aligned}
$$

选项给的是 $6x+8y-k-2=0$，常数项差 1。


####  Q10 等比数列 · 前 n 项和

题目给了一个等比数列 ${a_n}$，公比 $q \neq 1$，首项 $a_1 > 0$，满足 $2a_3 = a_1 + a_2$。记 $S_n$ 为前 $n$ 项和。                     
                                                                                                                                     
核心第一步：从 $2a_3 = a_1 + a_2$ 解出公比 $q = -\frac{1}{2}$。这一步定了，整个数列的通项和前 $n$ 
项和的表达式就锁死了，后面三个选项都在这一个表达式上做文章。

从条件推公比 

$$
2a_3 = a_1 + a_2
;\xrightarrow{a_n = a_1q^{n-1}};
2a_1q^2 = a_1 + a_1q
$$

$a_1 > 0$，两边约去 $a_1$：

$$
2q^2 = 1 + q
;\Rightarrow;
2q^2 - q - 1 = 0
;\Rightarrow;
(2q+1)(q-1) = 0
$$

$q \neq 1$（题设），故 $q = -\frac{1}{2}$。

A ✓。

通项立得：

$$
a_n = a_1\left(-\frac{1}{2}\right)^{n-1}
$$

前 $n$ 项和：

$$
S_n = \frac{a_1(1-q^n)}{1-q}
     = \frac{a_1\bigl[1-(-\frac{1}{2})^n\bigr]}{1-(-\frac{1}{2})}
     = \frac{a_1\bigl[1-(-\frac{1}{2})^n\bigr]}{\frac{3}{2}}
     = \frac{2a_1}{3}\Bigl[1-(-\frac{1}{2})^n\Bigr]
$$


##### B 选项：$S_n > \frac{2}{3}a_1$？ 

$$
S_n = \frac{2a_1}{3}\Bigl[1-(-\frac{1}{2})^n\Bigr]
$$

比较 $S_n$ 与 $\frac{2}{3}a_1$，等价于比较 $1-(-\frac{1}{2})^n$ 与 $1$，也就是看 $(-\frac{1}{2})^n$ 的正负。

 · $n$ 为奇数：$(-\frac{1}{2})^n < 0$，故 $1-(-\frac{1}{2})^n > 1$，$S_n > \frac{2a_1}{3}$ ✓
 
 你看，在这两个式子分别乘以$\frac{2}{3}a_1$，问题就凑出来了，这样也许更直观；
 
 · $n$ 为偶数：$(-\frac{1}{2})^n > 0$，故 $1-(-\frac{1}{2})^n < 1$，$S_n < \frac{2a_1}{3}$ ✗

偶数项时不等式反向，命题不恒成立。

B ✗。

##### C 选项：$2S_n + 2 = S_{n+1} + S_n$？ 

化简等式：

$$
2S_n + 2 = S_{n+1} + S_n
;\Rightarrow;
S_n + 2 = S_{n+1}
;\Rightarrow;
S_{n+1} - S_n = 2
$$

而 $S_{n+1} - S_n = a_{n+1}$，所以要求 $a_{n+1} = 2$。

但 $a_{n+1} = a_1(-\frac{1}{2})^n$，这是一个随 $n$ 变化的量（符号和绝对值都在变），不可能恒等于常数 $2$。

C ✗。

##### D 选项：$S_1+S_2+\cdots+S_n > \frac{2n}{3}a_1$ 

 Step 1：逐项写出 

记 $T_n = S_1 + S_2 + \cdots + S_n$。

由 $S_n$ 表达式，逐项写出前几项观察规律：

$$
\begin{aligned}
S_1 &= \frac{2a_1}{3}\Bigl[1-(-\tfrac{1}{2})\Bigr] \\
S_2 &= \frac{2a_1}{3}\Bigl[1-(-\tfrac{1}{2})^2\Bigr] \\
S_3 &= \frac{2a_1}{3}\Bigl[1-(-\tfrac{1}{2})^3\Bigr] \\
S_4 &= \frac{2a_1}{3}\Bigl[1-(-\tfrac{1}{2})^4\Bigr]
\end{aligned}
$$

 Step 2：求和并分组 

$$
\begin{aligned}
T_n &= \frac{2a_1}{3}\Bigl\{\bigl[1-(-\frac{1}{2})\bigr] + \bigl[1-(-\frac{1}{2})^2\bigr] + \cdots +
\bigl[1-(-\frac{1}{2})^n\bigr]\Bigr\} \\[4pt]
    &= \frac{2a_1}{3}\Bigl\{n - \bigl[(-\frac{1}{2}) + (-\frac{1}{2})^2 + \cdots + (-\frac{1}{2})^n\bigr]\Bigr\}
\end{aligned}
$$

括号内有 $n$ 个 $1$ 之和；以及一个等比数列的和。

 Step 3：等比数列求和 

$(-\frac{1}{2}) + (-\frac{1}{2})^2 + \cdots + (-\frac{1}{2})^n$ 是首项 $-\frac{1}{2}$、公比 $-\frac{1}{2}$ 的等比数列：

$$
\frac{-\frac{1}{2}\bigl[1-(-\frac{1}{2})^n\bigr]}{1-(-\frac{1}{2})}
= \frac{-\frac{1}{2}\bigl[1-(-\frac{1}{2})^n\bigr]}{\frac{3}{2}}
= -\frac{1}{3}\Bigl[1-(-\frac{1}{2})^n\Bigr]
$$

 Step 4：代回化简 
$$
\begin{aligned}
T_n &= \frac{2a_1}{3}\Bigl\{n - \Bigl(-\frac{1}{3}\bigl[1-(-\frac{1}{2})^n\bigr]\Bigr)\Bigr\} \\[4pt]
    &= \frac{2a_1}{3}\Bigl\{n + \frac{1}{3}\bigl[1-(-\frac{1}{2})^n\bigr]\Bigr\} \\[4pt]
    &= \frac{2n}{3}a_1 + \frac{2a_1}{9}\Bigl[1-(-\frac{1}{2})^n\Bigr]
\end{aligned}
$$

 Step 5：判断大小 

余项 $R_n = \frac{2a_1}{9}[1-(-\frac{1}{2})^n]$。对任意 $n \ge 1$：

 · $(-\frac{1}{2})^n$ 的绝对值 $\le \frac{1}{2} < 1$，故 $1-(-\frac{1}{2})^n > 0$ 恒成立
 · $a_1 > 0$ 恒成立

所以 $R_n > 0$ 对一切正整数 $n$ 成立。

$$
T_n = \frac{2n}{3}a_1 + R_n > \frac{2n}{3}a_1
$$

D ✓。





#### 第11题 抛物线性质 直线方程
拿到这道题先抓三个条件：

 1. 抛物线：$y^2 = 8x$ → $2p = 8$，$p = 4$，焦点 $F(2,0)$，准线 $x = -2$
 2. 直线 l：斜率 $k>0$，过 $(-1,0)$ → $l:; y = k(x+1)$
 3. 几何结构：等边三角形 ABC，顶点 A 在抛物线上，底边 BC 在直线 l 上


##### A 选项：准线方程 

▎ "抛物线 C 的准线方程为 x = -2"

$y^2 = 2px$ 型抛物线的准线为 $x = -\frac{p}{2}$。

$$y^2 = 8x \quad\Rightarrow\quad 2p = 8,; p = 4 \quad\Rightarrow\quad x = -\frac{p}{2} = -2$$

##### B 选项：直线与抛物线无交点
直线与抛物线的交点，是联立方程组的公共解。
$ y= k(x+1)$
$y^2 = 8x$

消去 $y$：$[k(x+1)]^2 = 8x$。

展开整理：

$$
k^2(x^2+2x+1) = 8x
;\rightarrow;
k^2 x^2 + 2k^2 x + k^2 - 8x = 0
;\rightarrow;
k^2 x^2 + (2k^2-8)x + k^2 = 0
$$

这是关于 $x$ 的一元二次方程。无交点 ⇔ 无实根 ⇔ 判别式 < 0。

$$
\Delta = (2k^2-8)^2 - 4 \cdot k^2 \cdot k^2
      = 4k^4 - 32k^2 + 64 - 4k^4
      = -32k^2 + 64
$$
Δ 展开时 $4k^4$ 抵消，最终只留 $-32k^2+64$。
$$
\Delta < 0 ;\Rightarrow; -32k^2 + 64 < 0 ;\Rightarrow; k^2 > 2
$$

题干给出 $k > 0$，所以 $k > \sqrt{2}$。

B ✓。

##### C选项
通过题目我们可
这是本题的核心难题。

 Step 1：确定切线 

相切 ⇔ 判别式 Δ = 0 ⇔ $-32k^2+64 = 0$ ⇔ $k = \sqrt{2}$（$k>0$ 舍去 $-\sqrt{2}$）。

此时 $l:; y = \sqrt{2}(x+1)$。代入联立方程：

$$2x^2 - 4x + 2 = 0 ;\rightarrow; x^2 - 2x + 1 = 0 ;\rightarrow; (x-1)^2 = 0$$

得到唯一交点 $x = 1$，$y = \sqrt{2}(1+1) = 2\sqrt{2}$。切点 $。


$$B(1, 2\sqrt{2})$$

 Step 2：用 C 选项的假设反推 A 

如果 C 成立——AB 过焦点 $F(2,0)$，那么 A、F、B 三点共线。

B 和 F 已知，直线 BF 的方程：

$$\text{斜率} = \frac{2\sqrt{2} - 0}{1 - 2} = -2\sqrt{2}
\quad\Rightarrow\quad
\text{BF}:; y = -2\sqrt{2}(x-2)$$

A 在 BF 上，同时又必须在抛物线 $y^2 = 8x$ 上。联立求交点：

$$
\bigl[-2\sqrt{2}(x-2)\bigr]^2 = 8x
;\rightarrow;
8(x-2)^2 = 8x
;\rightarrow;
x^2 - 5x + 4 = 0
$$

解得 $x = 1$ 或 $x = 4$。

 · $x = 1$：对应 B 自身（B 就是切点）
 · $x = 4$：则 $y = -2\sqrt{2}(4-2) = -4\sqrt{2}$

所以若 C 成立，必须取 $A(4, -4\sqrt{2})$。

 Step 3：验证等边条件 

有了 $A(4, -4\sqrt{2})$ 和 $B(1, 2\sqrt{2})$，计算 AB 长度：

$$
|AB|^2 = (4-1)^2 + (-4\sqrt{2} - 2\sqrt{2})^2
       = 9 + (-6\sqrt{2})^2
       = 9 + 72 = 81
$$

$$|AB| = 9$$

等边三角形要求 $|BC| = 9$。C 在直线 l 上，参数化 $C = (x, \sqrt{2}(x+1))$：

$$
|BC|^2 = (x-1)^2 + \bigl(\sqrt{2}(x+1) - 2\sqrt{2}\bigr)^2
       = (x-1)^2 + 2(x-1)^2
       = 3(x-1)^2
$$

令其等于 81：$3(x-1)^2 = 81 ;\Rightarrow; (x-1)^2 = 27 ;\Rightarrow; x = 1 \pm 3\sqrt{3}$。

$$C\bigl(1\pm3\sqrt{3},; 2\sqrt{2}\pm3\sqrt{6}\bigr)$$

 Step 4：验证第三条边 

现在验证 $|AC|$ 是否也是 9：

$$|AC|^2 = (4 - 1 \mp 3\sqrt{3})^2 + (-4\sqrt{2} - 2\sqrt{2} \mp 3\sqrt{6})^2$$

化简得 $162 + 54\sqrt{3} \approx 255 \neq 81$。矛盾！ 三角形不可能是等边的。

C ✗。

##### D 选项：k=2 时三角形面积的最小值 

 Step 1：设定和参数化 

$k=2$ 时，$l:; y = 2(x+1)$，即 $2x - y + 2 = 0$。

A 在抛物线上，参数化：令 $y_A = 4t$，则 $y_A^2 = 16t^2 = 8x_A ;\Rightarrow; x_A = 2t^2$。

$$A(2t^2,4t)$$

$t$ 取遍实数时，A 跑遍整条抛物线（$t<0$ 下半支，$t>0$ 上半支）。

 Step 2：距离 

A 到直线 l 的距离：

$$
d = \frac{|2\cdot 2t^2 - 4t + 2|}{\sqrt{5}}
  = \frac{|4t^2 - 4t + 2|}{\sqrt{5}}
$$

注意分子：

$$4t^2 - 4t + 2 = 4\Bigl(t - \frac{1}{2}\Bigr)^2 + 1 \ge 1 > 0$$

绝对值摘掉：$d = \dfrac{4t^2 - 4t + 2}{\sqrt{5}}$。

 Step 3：面积公式 

等边三角形中，高 $h = d$，边长 $s$ 与高的关系：$h = \frac{\sqrt{3}}{2}s ;\Rightarrow; s = \frac{2h}{\sqrt{3}}$。

面积：

$$
S = \frac{\sqrt{3}}{4}s^2
  = \frac{\sqrt{3}}{4} \cdot \frac{4h^2}{3}
  = \frac{\sqrt{3}}{3}h^2
  = \frac{\sqrt{3}}{3}d^2
$$

这个公式只需要一个顶点到对边的距离——等边三角形的高定了，面积就定了。

 Step 4：代入化简 

$$S = \frac{\sqrt{3}}{3} \cdot \frac{(4t^2 - 4t + 2)^2}{5}
   = \frac{\sqrt{3}}{15}(4t^2 - 4t + 2)^2$$

 Step 5：求最小值 

$$4t^2 - 4t + 2 = 4\Bigl(t - \frac{1}{2}\Bigr)^2 + 1 \ge 1$$

当 $t = \frac{1}{2}$ 时取等号。此时：

$$S_{\min} = \frac{\sqrt{3}}{15} \cdot 1^2 = \frac{\sqrt{3}}{15},\qquad A\Bigl(\frac{1}{2}, 2\Bigr)$$

D ✓。


#### Q12 等差数列求和                                         
                                                                                                                        
这是一道最短的填空题——等差数列，已知首项 $a_1 = -1$、第四项 $a_4 = 5$，求前六项和 $S_6$。                               
                                                                                                                        
两步走：$a_4$ 反推公差 $d$ → 代入求和公式。没有弯绕，没有分类讨论，唯一容易出错的是求和公式里 $n-1$ 和 $n$
的系数别代错。

 解题 

 Step 1：求公差 

等差数列通项：$a_n = a_1 + (n-1)d$。

$$
a_4 = a_1 + 3d
;\Rightarrow;
5 = -1 + 3d
;\Rightarrow;
d = 2
$$

 Step 2：求前六项和 

求和公式：$S_n = \frac{n}{2}\bigl(2a_1 + (n-1)d\bigr)$。

$$
S_6 = \frac{6}{2}\bigl(2\cdot(-1) + 5\cdot 2\bigr)
     = 3(-2 + 10)
     = 24
$$

或者先算通项再逐项加：$-1 + 1 + 3 + 5 + 7 + 9 = 24$，也能验算。


#### Q13 函数零点
                                                                                                                       
函数 $f(x) = 2^x + 2^{2-x} - m$ 含指数项 $2^x$ 和 $2^{2-x}$，两者互为倒数型（$2^{2-x} = 4 \cdot 2^{-x} =                
\frac{4}{2^x}$）。直接讨论 $f(x)$ 的零点很麻烦，令 $t = 2^x$ 换元，把问题转化为 $t + \frac{4}{t} = m$ 在 $t > 0$        
上有两个根。

这是典型的"对勾函数 + 水平线"问题：对勾函数 $y = t + \frac{4}{t}$ 的最小值由均值不等式给出，$m$
只要大于这个最小值就有两个交点——对应原来的两个 $x$。

整道题一步换元 + 一步均值 + 一步下结论，干净利落。


 Step 1：换元 

令 $t = 2^x > 0$。注意 $2^{2-x} = 2^2 \cdot 2^{-x} = \frac{4}{t}$。

$$
f(x) = 2^x + 2^{2-x} - m = t + \frac{4}{t} - m
$$

$f(x) = 0 ;\Leftrightarrow; t + \frac{4}{t} = m$。

$x$ 与 $t$ 一一对应（指数函数单调），所以 $f(x)$ 有两个零点 ⇔ 方程 $t + \frac{4}{t} = m$ 在 $(0, +\infty)$
上有两个不同的正根。

 ─────────────────────────────────────────────────────────────────────────────────────────────────────────────────

 Step 2：均值不等式 

对 $t > 0$：

$$
t + \frac{4}{t} \ge 2\sqrt{t \cdot \frac{4}{t}} = 2 \cdot 2 = 4
$$

当且仅当 $t = \frac{4}{t}$ 即 $t = 2$ 时取等号。

函数 $y = t + \frac{4}{t};(t>0)$ 在 $t \in (0,2]$ 上递减，在 $t \in [2,+\infty)$ 上递增，最小值为
$4$，

 ─────────────────────────────────────────────────────────────────────────────────────────────────────────────────

 Step 3：图像解读 

水平线 $y = m$ 与此曲线：

 · $m < 4$：无交点 → 无零点
 · $m = 4$：一个交点（$t = 2$）→ 一个零点
 · $m > 4$：两个交点（$t_1 < 2 < t_2$）→ 两个零点

$$
\therefore; m > 4
$$


#### Q14 球内接正三棱锥 讲解文稿

球体积给出 $R=\sqrt{3}$，四点共球，底面 $\triangle ABC$ 为正三角形，顶点 $D$ 到三底点等距（$DA=DB=DC=2$）。

平时练得的多了，你读完题目可能就会意识到，自己得分析一下球心是在三棱锥里面还是外面，侧棱已经固定，所以肯定只有一个答案；

关键结构：$D$ 到 $A,B,C$ 等距 ⇒ $D$ 在底面正上方，与底面中心 $H$ 的连线过球心 $O$——整道题的几何骨架就是**一条竖直线 $ODH$**。

在这条线上设 $OH = d$，底面外接圆半径 $r$，然后列两个勾股方程：

- $\triangle OHC$（球心到底面顶点）：$R^2 = d^2 + r^2$
- $\triangle DHC$（顶点到底面顶点）：$DC^2 = DH^2 + r^2$

两个方程消去 $r^2$ 得 $DH^2 - d^2 = 1$。接下来需要判断 $O$ 到底在 $D$ 和 $H$ 之间（球心在锥内），还是 $H$ 在 $O$ 和 $D$ 之间（球心在锥外）。通过反证排除前者，确立 $DH + d = \sqrt{3}$。后续解方程组、求 $r$、算面积，全是代数。

整道题的难度集中在**空间想象 + 位置判断**，计算量反而小。



##### Step 1：球体积 → 半径

$$
\frac{4}{3}\pi R^3 = 4\sqrt{3}\pi
\;\Rightarrow\;
R^3 = 3\sqrt{3}
\;\Rightarrow\;
R = \sqrt{3}
$$

##### Step 2：建立几何模型

$DA = DB = DC = 2$ ⇒ $D$ 到底面三顶点等距，故 $D$ 在 $\triangle ABC$ 所在平面的**正上方**（垂足 $H$ 为 $\triangle ABC$ 的中心）。

$A,B,C,D$ 都在球面上，球心 $O$ 在过 $H$ 且垂直于底面的直线上，即 **$O$、$H$、$D$ 三点共线**。

设 $\triangle ABC$ 外接圆半径为 $r$，$OH = d$（$d \ge 0$）。

##### Step 3：两个勾股方程

**方程①** — 直角 $\triangle OHC$（$OC = R$，$OH = d$，$HC = r$）：

$$
R^2 = d^2 + r^2 \;\Rightarrow\; 3 = d^2 + r^2
$$

**方程②** — 直角 $\triangle DHC$（$DC = 2$，$DH$ 未知，$HC = r$）：

$$
DC^2 = DH^2 + r^2 \;\Rightarrow\; 4 = DH^2 + r^2
$$

##### Step 4：消去 $r^2$

② − ① 得：

$$
DH^2 - d^2 = 1
$$

##### Step 5：判断 O 的位置

这是本题的关键一步。$D$ 在球顶（距 $O$ 为 $R = \sqrt{3}$），$H$ 在 $OD$ 直线上。

**假设 O 在 D 和 H 之间**（球心在锥内）：

则 $DH = DO + OH = R + d = \sqrt{3} + d$，代入上式：

$$
(\sqrt{3} + d)^2 - d^2 = 3 + 2\sqrt{3}d = 1
\;\Rightarrow\;
d = -\frac{1}{\sqrt{3}} < 0
$$

$d$ 为距离不能为负，矛盾！

**故 H 必在 O 和 D 之间**（球心在锥外、底面之下）。此时：

$$
DH = DO - OH = R - d = \sqrt{3} - d
$$

$$
DH + d = \sqrt{3}
$$

##### Step 6：解方程组

已知：

$$
\begin{cases}
DH^2 - d^2 = 1 \\[4pt]
DH + d = \sqrt{3}
\end{cases}
$$

由 $DH^2 - d^2 = (DH+d)(DH-d) = \sqrt{3}(DH-d) = 1$，得：

$$
DH - d = \frac{1}{\sqrt{3}} = \frac{\sqrt{3}}{3}
$$

与 $DH + d = \sqrt{3}$ 联立相加：

$$
2DH = \sqrt{3} + \frac{\sqrt{3}}{3} = \frac{4\sqrt{3}}{3}
\;\Rightarrow\;
DH = \frac{2\sqrt{3}}{3}
$$

代回①求 $r^2$：

$$
r^2 = 3 - d^2 = 3 - \left(\frac{\sqrt{3}}{3}\right)^2
     = 3 - \frac{1}{3}
     = \frac{8}{3}
$$

##### Step 7：等边三角形面积

等边三角形外接圆半径为 $r$ 时，边长 $a = \sqrt{3}\,r$，面积：

$$
S = \frac{\sqrt{3}}{4}a^2
  = \frac{\sqrt{3}}{4} \cdot 3r^2
  = \frac{3\sqrt{3}}{4}r^2
$$

代入 $r^2 = \dfrac{8}{3}$：

$$
S = \frac{3\sqrt{3}}{4} \cdot \frac{8}{3}
  = 2\sqrt{3}
$$

**答案：$2\sqrt{3}$**


##### 值得记住的技巧

- **DA = DB = DC → 顶点在底面中心正上方**：空间几何高频结论。
- **球心位置的反证**：假设 O 在锥内推出 $d<0$ 矛盾，确立 $DH + d = R$。不靠直觉，靠计算。
- **面积的快捷公式**：$S = \frac{3\sqrt{3}}{4}r^2$，等边三角形外接圆半径到面积，比"边长 → 面积"少一步。




#### Q15 频率分布直方图与二项分布

##### 开场引入

同学们，我们来看一道关于**频率分布直方图**的统计题。这道题分两问：第一问要求我们根据直方图求第一四分位数和中位数；第二问则把概率估计与二项分布结合起来。整道题思路清晰，计算量也不大，关键是要理解频率分布直方图中"面积表示频率"这个核心概念。

---

##### 题目条件梳理

先看一下题目给出的频率分布直方图（组距为 10）：

| 时间区间（天） | 频率/组距 | 频率 |
|:---:|:---:|:---:|
| [345, 355) | 0.005 | 0.05 |
| [355, 365) | 0.010 | 0.10 |
| [365, 375) | 0.020 | 0.20 |
| [375, 385) | 0.025 | 0.25 |
| [385, 395) | 0.015 | 0.15 |
| [395, 405) | 0.015 | 0.15 |
| [405, 415) | 0.005 | 0.05 |
| [415, 425) | 0.005 | 0.05 |

**【教师讲解】**

在频率分布直方图中，**小矩形的面积 = 频率/组距 × 组距 = 频率**。所以每一组的频率，就是用该组矩形的高乘以组距。

这里组距都是 10，所以各组频率分别为：

$$0.005\times10=0.05,\quad 0.010\times10=0.10,\quad 0.020\times10=0.20,\quad \cdots$$

全部加起来：$0.05+0.10+0.20+0.25+0.15+0.15+0.05+0.05=1$，验证无误。

---

##### 第（1）问：第一四分位数和中位数

**【教师讲解】**

第一四分位数 $Q_1$ 就是第 25 百分位数，中位数就是第 50 百分位数。求法类似，都是通过累积频率找到对应的组，再用线性插值计算具体数值。

**求第一四分位数 $Q_1$：**

我们需要找到累积频率达到 0.25 的位置。

- 第 1 组 [345, 355)：累积频率 0.05；
- 第 2 组 [355, 365)：累积频率 $0.05+0.10=0.15$；
- 第 3 组 [365, 375)：累积频率 $0.15+0.20=0.35$。

0.25 落在第 3 组 [365, 375) 内。说明 $Q_1$ 在 365 到 375 之间。

用线性插值公式：

$$Q_1=365+10\times\frac{0.25-0.15}{0.20}=365+10\times\frac{0.10}{0.20}=365+5=370$$

所以第一四分位数为 **370 天**。

**求中位数：**

中位数对应累积频率 0.50。

- 前 3 组累积频率：$0.05+0.10+0.20=0.35$；
- 第 4 组 [375, 385)：累积频率 $0.35+0.25=0.60$。

0.50 落在第 4 组 [375, 385) 内。

用线性插值公式：

$$中位数=375+10\times\frac{0.50-0.35}{0.25}=375+10\times\frac{0.15}{0.25}=375+6=381$$

所以中位数为 **381 天**。

**【板书要点】**

- 频率 = 频率/组距 × 组距；
- $Q_1=365+10\times\dfrac{0.25-0.15}{0.20}=370$；
- 中位数 $=375+10\times\dfrac{0.50-0.35}{0.25}=381$。

**【提醒学生】**

求百分位数时，一定要先看累积频率落在哪一组，再用"组下限 + 组距 × (目标累积频率 - 前一组累积频率) / 该组频率"这个公式。不要直接用频率除以 2 去找中点。

---

##### 第（2）问：概率估计与二项分布

**第（i）小题：求 $\hat{p}$**

$\hat{p}$ 表示首次故障时间小于 365 天的概率估计值。

"小于 365 天"对应的就是前两组：[345, 355) 和 [355, 365)。

所以：

$$\hat{p}=0.05+0.10=0.15$$

**第（ii）小题：求 $E(X)$ 和 $D(X)$**

工厂销售 100 件产品，$X$ 表示这 100 件中首次故障时间小于 365 天的件数。每件产品可以看作一次独立重复试验，"成功"就是故障时间小于 365 天，成功概率为 $\hat{p}=0.15$。

因此：

$$X\sim B(100,\,0.15)$$

对于二项分布 $X\sim B(n,p)$，期望和方差公式分别为：

$$E(X)=np,\quad D(X)=np(1-p)$$

代入 $n=100$，$p=0.15$：

$$E(X)=100\times0.15=15$$

$$D(X)=100\times0.15\times0.85=12.75$$

**【板书要点】**

- $\hat{p}=0.05+0.10=0.15$；
- $X\sim B(100,0.15)$；
- $E(X)=np=15$；
- $D(X)=np(1-p)=12.75$。

**【提醒学生】**

判断一个随机变量是否服从二项分布，要检查三个条件：

1. **独立性**：各件产品是否相互独立；
2. **重复性**：试验次数 $n$ 固定；
3. **二值性**：每次试验只有"成功"和"失败"两种结果，且成功概率相同。

本题中 100 件产品可以近似看作独立重复试验，所以用二项分布是合理的。

---

##### 课堂小结

这道题主要考查了三个知识点：

1. **频率分布直方图的读图能力**：要会通过"频率/组距"计算每组的频率，并通过累积频率找到对应的分位数。
2. **百分位数的计算**：关键是确定目标累积频率落在哪一组，再用线性插值公式精确计算。
3. **二项分布的识别与应用**：把"100 件产品中有多少件在 365 天内出现故障"转化为 $B(100,0.15)$，再用期望和方差公式求解。

**易错点提醒**：

- 不要把"频率/组距"直接当成频率，记得乘以组距；
- 求中位数和四分位数时，注意是用累积频率去找位置，不是用频率去找；
- 二项分布的方差公式是 $np(1-p)$，不要漏掉 $(1-p)$。

好，这道题就讲到这里。大家课后可以把直方图自己画一遍，标出每一组的频率和累积频率，这样更不容易出错。




#### Q16 三棱锥中的垂直证明与线面角

##### 开场引入
这道题分两问：第一问是证明两条异面直线垂直，第二问是求直线与平面所成角的正弦值。这类题目在非常典型，核心思路就是**线面垂直的判定与性质**，以及**空间向量求线面角**。我们一起来分析。

---

##### 题目条件梳理

如图，三棱锥 $A-BCD$ 中：

- 点 $E$ 在 $BD$ 上；
- $AE\perp CE$，$AE\perp DE$；
- $CD\perp AD$。

第（1）问要求证明 $CD\perp AB$；
第（2）问给出 $DE=2$，$BE=1$，$AE=\sqrt{2}$，$CD=2\sqrt{3}$，求直线 $AD$ 与平面 $ABC$ 所成角的正弦值。

**【教师讲解】**

看到 $AE\perp CE$ 和 $AE\perp DE$，大家应该立刻想到：如果一条直线垂直于平面内的两条相交直线，那么这条直线就垂直于这个平面。这里 $CE$ 和 $DE$ 相交于 $E$，所以 $AE\perp$ 平面 $BCD$。这是第一问证明的突破口。

---

##### 第（1）问：证明 $CD\perp AB$

**【教师讲解】**

我们要证明 $CD\perp AB$。注意到 $CD$ 和 $AB$ 是异面直线，直接证明它们垂直不太方便。通常的思路是：证明 $CD$ 垂直于 $AB$ 所在的某个平面。

具体步骤如下：

**第一步：证明 $AE\perp$ 平面 $BCD$。**

已知 $AE\perp CE$，$AE\perp DE$，且 $CE\cap DE=E$。

根据线面垂直的判定定理：如果一条直线与一个平面内的两条相交直线都垂直，那么这条直线与这个平面垂直。

所以：

$$AE\perp\text{平面 }BCD$$

**第二步：证明 $AE\perp CD$。**

因为 $CD\subset$ 平面 $BCD$，而 $AE\perp$ 平面 $BCD$，所以：

$$AE\perp CD$$

**第三步：证明 $CD\perp$ 平面 $ADE$。**

已知 $CD\perp AD$，又刚刚证明了 $CD\perp AE$，且 $AD\cap AE=A$。

所以 $CD$ 垂直于平面 $ADE$ 内的两条相交直线 $AD$ 和 $AE$，因此：

$$CD\perp\text{平面 }ADE$$

**第四步：证明 $CD\perp AB$。**

因为点 $E$ 在 $BD$ 上，所以直线 $BD$ 在平面 $ADE$ 内，从而点 $B$ 也在平面 $ADE$ 内。

因此 $AB\subset$ 平面 $ADE$。

又因为 $CD\perp$ 平面 $ADE$，所以 $CD$ 垂直于平面 $ADE$ 内的任意一条直线，当然包括 $AB$：

$$CD\perp AB$$

**【板书要点】**

- $AE\perp CE$，$AE\perp DE$ $\Rightarrow$ $AE\perp$ 平面 $BCD$；
- $CD\subset$ 平面 $BCD$ $\Rightarrow$ $AE\perp CD$；
- $CD\perp AD$，$CD\perp AE$ $\Rightarrow$ $CD\perp$ 平面 $ADE$；
- $AB\subset$ 平面 $ADE$ $\Rightarrow$ $CD\perp AB$。

**【提醒学生】**

证明异面直线垂直时，优先考虑"线面垂直 $\Rightarrow$ 线线垂直"这条路。关键是找到一个包含其中一条直线且与另一条直线垂直的平面。

---

##### 第（2）问：求 $AD$ 与平面 $ABC$ 所成角的正弦值

**【教师讲解】**

第二问给出了具体数据：$DE=2$，$BE=1$，$AE=\sqrt{2}$，$CD=2\sqrt{3}$。要求直线 $AD$ 与平面 $ABC$ 所成角的正弦值。

这种求空间角的问题，最稳妥的方法是什么？对，建立空间直角坐标系，用向量法求解。

**第一步：求 $AD$ 的长度。**

由第（1）问我们已经知道 $AE\perp$ 平面 $BCD$，所以 $AE\perp DE$。

在直角三角形 $ADE$ 中：

$$AD=\sqrt{AE^2+DE^2}=\sqrt{(\sqrt{2})^2+2^2}=\sqrt{2+4}=\sqrt{6}$$

**第二步：建立空间直角坐标系。**

以 $D$ 为原点，分别以 $DC$、$DB$、$AE$ 的方向为 $x$、$y$、$z$ 轴正方向建立坐标系。

为什么是这三条线？因为：

- $AE\perp$ 平面 $BCD$，所以 $AE$ 垂直于平面 $BCD$ 内的任何直线，自然可以作为 $z$ 轴；
- 在平面 $BCD$ 内，$CD\perp BD$ 吗？我们由第（1）问知道 $CD\perp$ 平面 $ADE$，而 $BD\subset$ 平面 $ADE$，所以 $CD\perp BD$。因此 $DC$ 和 $DB$ 互相垂直，可以作为 $x$ 轴和 $y$ 轴。

于是各点坐标为：

- $D(0,0,0)$；
- $C(2\sqrt{3},0,0)$，因为 $CD=2\sqrt{3}$；
- $B(0,3,0)$，因为 $DB=DE+EB=2+1=3$；
- $A(0,2,\sqrt{2})$，因为 $E$ 在 $DB$ 上，$DE=2$，所以 $E$ 的坐标是 $(0,2,0)$，又 $AE=\sqrt{2}$ 且 $AE\perp$ 平面 $BCD$，所以 $A$ 在 $E$ 正上方，$A(0,2,\sqrt{2})$。

**第三步：求平面 $ABC$ 的法向量。**

我们需要平面 $ABC$ 内的两个不共线向量。

$$\overrightarrow{AB}=B-A=(0,3,0)-(0,2,\sqrt{2})=(0,1,-\sqrt{2})$$

$$\overrightarrow{AC}=C-A=(2\sqrt{3},0,0)-(0,2,\sqrt{2})=(2\sqrt{3},-2,-\sqrt{2})$$

设平面 $ABC$ 的法向量为 $\boldsymbol{n}=(x,y,z)$，则：

$$\boldsymbol{n}\cdot\overrightarrow{AB}=0,\quad \boldsymbol{n}\cdot\overrightarrow{AC}=0$$

即：

$$\begin{cases}y-\sqrt{2}z=0\\2\sqrt{3}x-2y-\sqrt{2}z=0\end{cases}$$

由第一个方程得 $y=\sqrt{2}z$。令 $z=1$，则 $y=\sqrt{2}$。

代入第二个方程：

$$2\sqrt{3}x-2\sqrt{2}-\sqrt{2}=0$$

$$2\sqrt{3}x=3\sqrt{2}$$

$$x=\frac{3\sqrt{2}}{2\sqrt{3}}=\frac{\sqrt{6}}{2}$$

所以一个法向量可以取：

$$\boldsymbol{n}=\left(\frac{\sqrt{6}}{2},\sqrt{2},1\right)$$

为了方便计算，我们可以乘以 2，得到：

$$\boldsymbol{n}=(\sqrt{6},2\sqrt{2},2)$$

当然，也可以用叉乘直接得到：

$$\boldsymbol{n}=\overrightarrow{AB}\times\overrightarrow{AC}$$

不管怎样，最终计算结果是一样的。

**第四步：计算线面角的正弦值。**

直线 $AD$ 与平面 $ABC$ 所成角设为 $\theta$，则：

$$\sin\theta=\frac{|\overrightarrow{AD}\cdot\boldsymbol{n}|}{|\overrightarrow{AD}|\cdot|\boldsymbol{n}|}$$

其中 $\overrightarrow{AD}=(0,2,\sqrt{2})$。

计算点积：

$$\overrightarrow{AD}\cdot\boldsymbol{n}=0\times\sqrt{6}+2\times2\sqrt{2}+\sqrt{2}\times2=4\sqrt{2}+2\sqrt{2}=6\sqrt{2}$$

计算模长：

$$|\overrightarrow{AD}|=\sqrt{0^2+2^2+(\sqrt{2})^2}=\sqrt{6}$$

$$|\boldsymbol{n}|=\sqrt{(\sqrt{6})^2+(2\sqrt{2})^2+2^2}=\sqrt{6+8+4}=\sqrt{18}=3\sqrt{2}$$

所以：

$$\sin\theta=\frac{6\sqrt{2}}{\sqrt{6}\cdot3\sqrt{2}}=\frac{6\sqrt{2}}{3\sqrt{12}}=\frac{6\sqrt{2}}{3\times2\sqrt{3}}=\frac{6\sqrt{2}}{6\sqrt{3}}=\frac{\sqrt{2}}{\sqrt{3}}=\frac{\sqrt{6}}{3}$$

**【板书要点】**

- $AD=\sqrt{6}$；
- 坐标系：$D(0,0,0)$，$C(2\sqrt{3},0,0)$，$B(0,3,0)$，$A(0,2,\sqrt{2})$；
- $\overrightarrow{AB}=(0,1,-\sqrt{2})$，$\overrightarrow{AC}=(2\sqrt{3},-2,-\sqrt{2})$；
- 法向量 $\boldsymbol{n}=(\sqrt{6},2\sqrt{2},2)$；
- $\sin\theta=\dfrac{\sqrt{6}}{3}$。

**【提醒学生】**

1. 建立坐标系前，一定要确认三条坐标轴两两垂直。本题中 $AE\perp$ 平面 $BCD$ 保证了 $AE$ 可以作为 $z$ 轴；$CD\perp BD$ 保证了 $DC$ 和 $DB$ 可以作为 $x$、$y$ 轴。
2. 求线面角时，用的是直线方向向量与平面法向量夹角的**余角**，所以公式中是 $|\cos\langle\overrightarrow{AD},\boldsymbol{n}\rangle|$，也就是正弦值。
3. 法向量不唯一，方向不同不影响最终结果，因为取了绝对值。

---

##### 课堂小结

这道题考查了立体几何中两个核心问题：

1. **垂直关系的证明**：通过"线面垂直"作为桥梁，把异面直线的垂直问题转化为线面垂直问题。关键是找到合适的平面。
2. **空间角的计算**：建立空间直角坐标系，利用向量法求直线与平面所成角。关键是正确写出各点坐标，并准确计算法向量。

**易错点提醒**：

- 证明 $CD\perp AB$ 时，不要说成"因为 $CD\perp AD$，所以 $CD\perp AB$"，这是不严谨的。必须证明 $CD$ 垂直于 $AB$ 所在的平面。
- 建系时，要确保 $x$、$y$、$z$ 轴两两垂直。如果坐标系建错了，后面全部都会错。
- 计算线面角时，公式中是 $\sin\theta=\dfrac{|\overrightarrow{AD}\cdot\boldsymbol{n}|}{|\overrightarrow{AD}|\cdot|\boldsymbol{n}|}$，不要和线线角的余弦公式混淆。

好，这道题就讲到这里。大家课后可以把第（2）问用几何法再做一遍，看看能不能用等体积法求出点 $D$ 到平面 $ABC$ 的距离，从而得到线面角的正弦值。








#### Q17 解三角形：钝角三角形判定与周长计算

##### 开场引入

同学们，我们来看一道解三角形的题目。这道题条件非常简洁，但综合性很强：第一问要求证明三角形是钝角三角形，第二问在已知面积的条件下求周长。整道题的核心是**正弦定理、余弦定理的灵活运用**，以及对三角恒等变换的熟练掌握。我们一起来看。

---

##### 题目条件梳理

在 $\triangle ABC$ 中：

- $\cos B=\dfrac{3}{4}$；
- $\cos^2(A+C)+\sin A\sin C=1$。

第（1）问：证明 $\triangle ABC$ 为钝角三角形；
第（2）问：若面积为 $\dfrac{\sqrt{7}}{4}$，求周长。

**【教师讲解】**

看到 $\cos^2(A+C)$，大家应该立刻想到三角形内角和：$A+B+C=\pi$，所以 $A+C=\pi-B$。那么 $\cos(A+C)=-\cos B$，从而 $\cos^2(A+C)=\cos^2 B$。这是第一问的突破口。

---

##### 第（1）问：证明 $\triangle ABC$ 为钝角三角形

**【教师讲解】**

我们要证明三角形是钝角三角形，就是要证明有一个内角的余弦值为负。通常的做法是：通过已知条件建立边之间的关系，再用余弦定理判断最大边所对角的正负。

**第一步：利用三角形内角和化简条件。**

因为 $A+B+C=\pi$，所以：

$$A+C=\pi-B$$

因此：

$$\cos(A+C)=\cos(\pi-B)=-\cos B$$

两边平方：

$$\cos^2(A+C)=\cos^2 B$$

**第二步：建立 $\sin A\sin C$ 与 $\sin^2 B$ 的关系。**

已知：

$$\cos^2(A+C)+\sin A\sin C=1$$

将 $\cos^2(A+C)=\cos^2 B$ 代入：

$$\cos^2 B+\sin A\sin C=1$$

移项：

$$\sin A\sin C=1-\cos^2 B=\sin^2 B$$

**第三步：由正弦定理得到边的关系。**

正弦定理告诉我们：

$$\frac{a}{\sin A}=\frac{b}{\sin B}=\frac{c}{\sin C}=2R$$

所以：

$$\sin A=\frac{a}{2R},\quad \sin B=\frac{b}{2R},\quad \sin C=\frac{c}{2R}$$

代入 $\sin A\sin C=\sin^2 B$：

$$\frac{a}{2R}\cdot\frac{c}{2R}=\left(\frac{b}{2R}\right)^2$$

整理得：

$$ac=b^2 \tag{1}$$

这是一个非常重要的结论，大家一定要记住。

**第四步：结合余弦定理建立 $a,c$ 的关系。**

由余弦定理：

$$b^2=a^2+c^2-2ac\cos B$$

已知 $\cos B=\dfrac{3}{4}$，代入得：

$$b^2=a^2+c^2-2ac\cdot\frac{3}{4}=a^2+c^2-\frac{3}{2}ac$$

又因为 $b^2=ac$，所以：

$$ac=a^2+c^2-\frac{3}{2}ac$$

移项：

$$a^2+c^2=ac+\frac{3}{2}ac=\frac{5}{2}ac \tag{2}$$

**第五步：求 $a:c$ 的比值。**

方程 (2) 两边同除以 $c^2$（$c>0$），令 $t=\dfrac{a}{c}$：

$$\left(\frac{a}{c}\right)^2+1=\frac{5}{2}\cdot\frac{a}{c}$$

即：

$$t^2+1=\frac{5}{2}t$$

两边同乘 2：

$$2t^2+2=5t$$

整理：

$$2t^2-5t+2=0$$

因式分解：

$$(2t-1)(t-2)=0$$

解得：

$$t=2\quad\text{或}\quad t=\frac{1}{2}$$

即：

$$\frac{a}{c}=2\quad\text{或}\quad\frac{a}{c}=\frac{1}{2}$$

**第六步：判断三角形的形状。**

**情形一：** $\dfrac{a}{c}=2$，即 $a=2c$。

由 $b^2=ac=2c^2$，得 $b=\sqrt{2}\,c$。

三边分别为：$a=2c$，$b=\sqrt{2}\,c$，$c=c$。

比较大小：$2c>\sqrt{2}\,c>c$，所以最大边为 $a$，最大角为 $A$。

用余弦定理计算 $\cos A$：

$$\cos A=\frac{b^2+c^2-a^2}{2bc}=\frac{2c^2+c^2-4c^2}{2\cdot\sqrt{2}\,c\cdot c}=\frac{-c^2}{2\sqrt{2}\,c^2}=-\frac{1}{2\sqrt{2}}<0$$

所以角 $A$ 为钝角。

**情形二：** $\dfrac{a}{c}=\dfrac{1}{2}$，即 $c=2a$。

由 $b^2=ac=2a^2$，得 $b=\sqrt{2}\,a$。

三边分别为：$a=a$，$b=\sqrt{2}\,a$，$c=2a$。

最大边为 $c$，最大角为 $C$。

同理可算得：

$$\cos C=\frac{a^2+b^2-c^2}{2ab}=\frac{a^2+2a^2-4a^2}{2\cdot a\cdot\sqrt{2}\,a}=\frac{-a^2}{2\sqrt{2}\,a^2}=-\frac{1}{2\sqrt{2}}<0$$

所以角 $C$ 为钝角。

综上，无论是哪种情形，$\triangle ABC$ 都有一个钝角，因此它是**钝角三角形**。

**【板书要点】**

- $A+C=\pi-B$ $\Rightarrow$ $\cos^2(A+C)=\cos^2 B$；
- $\sin A\sin C=\sin^2 B$ $\Rightarrow$ $ac=b^2$；
- $b^2=a^2+c^2-\dfrac{3}{2}ac$ $\Rightarrow$ $a^2+c^2=\dfrac{5}{2}ac$；
- $2\left(\dfrac{a}{c}\right)^2-5\dfrac{a}{c}+2=0$ $\Rightarrow$ $\dfrac{a}{c}=2$ 或 $\dfrac{1}{2}$；
- 两种情形均有一个钝角，得证。

**【提醒学生】**

证明钝角三角形时，不要试图证明某个特定角是钝角。因为 $a$ 和 $c$ 的大小关系不确定，可能是 $A$ 钝，也可能是 $C$ 钝。所以要分两种情况讨论，或者找到最大边所对的角来判断。

---

##### 第（2）问：求 $\triangle ABC$ 的周长

**【教师讲解】**

第二问给出了面积，要求周长。我们知道周长 $=a+b+c$，所以需要分别求出 $a+c$ 和 $b$。

**第一步：由面积求 $ac$。**

三角形面积公式：

$$S=\frac{1}{2}ac\sin B$$

已知 $S=\dfrac{\sqrt{7}}{4}$，$\cos B=\dfrac{3}{4}$，所以：

$$\sin B=\sqrt{1-\cos^2 B}=\sqrt{1-\frac{9}{16}}=\sqrt{\frac{7}{16}}=\frac{\sqrt{7}}{4}$$

代入面积公式：

$$\frac{1}{2}ac\cdot\frac{\sqrt{7}}{4}=\frac{\sqrt{7}}{4}$$

两边约去 $\dfrac{\sqrt{7}}{4}$：

$$\frac{1}{2}ac=1$$

所以：

$$ac=2$$

**第二步：求 $b$。**

由第（1）问得到的结论 $b^2=ac$，代入 $ac=2$：

$$b^2=2$$

所以：

$$b=\sqrt{2}$$

**第三步：求 $a+c$。**

由第（1）问还有 $a^2+c^2=\dfrac{5}{2}ac$。代入 $ac=2$：

$$a^2+c^2=\frac{5}{2}\times2=5$$

利用完全平方公式：

$$(a+c)^2=a^2+c^2+2ac=5+4=9$$

因为 $a+c>0$，所以：

$$a+c=3$$

**第四步：求周长。**

$$\text{周长}=a+b+c=(a+c)+b=3+\sqrt{2}$$

**【板书要点】**

- $\sin B=\dfrac{\sqrt{7}}{4}$；
- $S=\dfrac{1}{2}ac\sin B=\dfrac{\sqrt{7}}{4}$ $\Rightarrow$ $ac=2$；
- $b^2=ac=2$ $\Rightarrow$ $b=\sqrt{2}$；
- $a^2+c^2=\dfrac{5}{2}ac=5$；
- $(a+c)^2=9$ $\Rightarrow$ $a+c=3$；
- 周长 $=3+\sqrt{2}$。

**【提醒学生】**

求周长时，通常不需要分别求出 $a$ 和 $c$，只需要求出 $a+c$ 即可。这道题中 $a$ 和 $c$ 的具体值有两种可能（$a=2,c=1$ 或 $a=1,c=2$），但 $a+c$ 都是 3，所以不影响周长。

---

##### 课堂小结

这道题主要考查了以下几个知识点：

1. **三角恒等变换**：利用 $A+C=\pi-B$ 将 $\cos^2(A+C)$ 转化为 $\cos^2 B$；
2. **正弦定理**：将角的正弦关系转化为边的关系，得到 $ac=b^2$；
3. **余弦定理**：建立 $a^2+c^2$ 与 $ac$ 的关系；
4. **钝角三角形的判定**：通过最大边所对角的余弦值符号来判断；
5. **面积公式与完全平方公式**：由面积求出 $ac$，再求出 $a+c$ 和 $b$，得到周长。

**易错点提醒**：

- 由 $\sin A\sin C=\sin^2 B$ 得到 $ac=b^2$ 时，要注意正弦定理的形式，不要漏掉 $2R$ 的平方；
- 解出 $\dfrac{a}{c}=2$ 或 $\dfrac{1}{2}$ 后，两种情况都要讨论，不能默认 $a>c$；
- 求 $\sin B$ 时，因为 $B$ 是三角形内角，所以 $\sin B>0$，取正根；
- 开平方求 $a+c$ 时，要注意 $a+c>0$，舍去负根。

好，这道题就讲到这里。大家课后可以思考一下：如果把条件中的 $\cos B=\dfrac{3}{4}$ 改成其他值，结论是否仍然成立？这有助于加深对题意的理解。





#### Q18 椭圆轨迹方程 二次曲线的分类

这道题以椭圆为背景，但核心考查的是**轨迹方程**和**二次曲线的分类**。整道题共有三小问，难度逐步提升。我们先整体读一下题目。

> 椭圆 $E:\dfrac{x^2}{a^2}+y^2=1\ (a>1)$，过 $E$ 的右焦点 $F$ 且与 $x$ 轴垂直的直线被 $E$ 截得的长度为 $\sqrt{2}$。

第一问求离心率；第二问引入了一个动点 $P$，要求我们求 $P$ 的轨迹，并进一步研究轨迹的中心问题。

好，我们先从第一问开始。

---

##### 第（1）问：求椭圆 $E$ 的离心率

**【教师讲解】**

第一问比较基础，主要考察椭圆的几何性质。大家要特别注意题目中的一句话："过右焦点 $F$ 且与 $x$ 轴垂直的直线被 $E$ 截得的长度为 $\sqrt{2}$"。

这条直线是什么？就是椭圆的**通径**所在直线。

我们设右焦点为 $F(c,0)$，其中 $c^2=a^2-b^2$。在这道题里，$b^2=1$，所以 $c^2=a^2-1$。

把 $x=c$ 代入椭圆方程 $\dfrac{x^2}{a^2}+y^2=1$，得到：

$$\frac{c^2}{a^2}+y^2=1$$

所以 $y^2=1-\dfrac{c^2}{a^2}=\dfrac{a^2-c^2}{a^2}=\dfrac{b^2}{a^2}=\dfrac{1}{a^2}$。

因此 $y=\pm\dfrac{1}{a}$。也就是说，这条直线与椭圆的两个交点是 $\left(c,\dfrac{1}{a}\right)$ 和 $\left(c,-\dfrac{1}{a}\right)$。

弦长就是这两个点之间的距离，等于 $\dfrac{2}{a}$。

题目告诉我们弦长等于 $\sqrt{2}$，所以：

$$\frac{2}{a}=\sqrt{2}$$

解得 $a=\sqrt{2}$。

那么 $c^2=a^2-1=2-1=1$，所以 $c=1$。

离心率 $e=\dfrac{c}{a}=\dfrac{1}{\sqrt{2}}=\dfrac{\sqrt{2}}{2}$。

**【板书要点】**

- 通径长公式：$\dfrac{2b^2}{a}$；
- 本题 $b^2=1$，所以通径长为 $\dfrac{2}{a}$；
- $a=\sqrt{2}$，$c=1$，$e=\dfrac{\sqrt{2}}{2}$。

**【提醒学生】**

注意题目给的是整条弦的长度，不是半个弦长。有些同学可能会把 $\dfrac{1}{a}=\sqrt{2}$ 算出来，那就错了。

---

##### 第（2）问第（i）小题：求轨迹 $M$ 的方程并说明曲线类型

**【教师讲解】**

好，我们进入第二问。这一问是整道题的核心，大家要认真听。

题目说：$O$ 是坐标原点，给定一点 $G(t_0,0)$，其中 $t_0\neq0$。点 $A(x_0,y_0)$ 在椭圆 $E$ 上运动，且 $y_0\neq0$。过 $A$ 作 $y$ 轴的垂线，垂足为 $B$。直线 $AO$ 与 $GB$ 相交于点 $P$。当 $A$ 在椭圆上运动时，$P$ 的轨迹为 $M$。

我们的目标是求 $M$ 的方程，并说明它是什么曲线。

**第一步：确定相关点的坐标。**

大家看，$A(x_0,y_0)$。因为 $AB$ 垂直于 $y$ 轴，所以 $B$ 的纵坐标和 $A$ 相同，横坐标为 $0$。也就是说，$B(0,y_0)$。

$O$ 是原点，$G(t_0,0)$ 是定点。

设 $P(x,y)$，这就是我们要研究的动点。

**第二步：利用共线关系建立方程。**

$P$ 点在直线 $AO$ 上，也就是说 $O,P,A$ 三点共线。那么 $OP$ 和 $OA$ 的斜率相等：

$$\frac{y}{x}=\frac{y_0}{x_0}$$

交叉相乘，得到：

$$x_0y=xy_0 \tag{1}$$

同样地，$P$ 点也在直线 $GB$ 上，也就是说 $G,P,B$ 三点共线。$G(t_0,0)$，$B(0,y_0)$，所以直线 $GB$ 的斜率为：

$$k_{GB}=\frac{y_0-0}{0-t_0}=-\frac{y_0}{t_0}$$

而 $P(x,y)$ 在 $GB$ 上，所以 $GP$ 的斜率也是 $-\dfrac{y_0}{t_0}$：

$$\frac{y-0}{x-t_0}=-\frac{y_0}{t_0}$$

即：

$$\frac{y}{x-t_0}=-\frac{y_0}{t_0}$$

交叉相乘，得到：

$$t_0y=-y_0(x-t_0) \tag{2}$$

**第三步：从方程 (1)(2) 中解出 $x_0,y_0$。**

由方程 (1)，$x_0y=xy_0$，我们可以得到 $y_0=\dfrac{x_0y}{x}$，这里默认 $x\neq0$。

把它代入方程 (2)：

$$t_0y=-\frac{x_0y}{x}(x-t_0)$$

因为 $y_0\neq0$，一般情况下 $y\neq0$，两边可以约去 $y$：

$$t_0=-\frac{x_0(x-t_0)}{x}$$

整理：

$$t_0x=-x_0x+x_0t_0$$

$$x_0(t_0-x)=t_0x$$

所以：

$$x_0=\frac{t_0x}{t_0-x}$$

再代回 $y_0=\dfrac{x_0y}{x}$，得到：

$$y_0=\frac{t_0y}{t_0-x}$$

**第四步：代入椭圆方程消参。**

因为 $A(x_0,y_0)$ 在椭圆 $E:\dfrac{x^2}{2}+y^2=1$ 上，所以：

$$\frac{x_0^2}{2}+y_0^2=1$$

把 $x_0=\dfrac{t_0x}{t_0-x}$，$y_0=\dfrac{t_0y}{t_0-x}$ 代入：

$$\frac{1}{2}\left(\frac{t_0x}{t_0-x}\right)^2+\left(\frac{t_0y}{t_0-x}\right)^2=1$$

两边同乘 $(t_0-x)^2$：

$$\frac{1}{2}t_0^2x^2+t_0^2y^2=(t_0-x)^2$$

展开右边：

$$(t_0-x)^2=t_0^2-2t_0x+x^2$$

整理：

$$\frac{1}{2}t_0^2x^2+t_0^2y^2=t_0^2-2t_0x+x^2$$

把所有项移到左边：

$$\frac{1}{2}t_0^2x^2-x^2+t_0^2y^2+2t_0x-t_0^2=0$$

两边同乘 2：

$$(t_0^2-2)x^2+2t_0^2y^2+4t_0x-2t_0^2=0$$

这就是轨迹 $M$ 的方程。

**第五步：判断曲线类型。**

这是一个二次曲线方程，不含 $xy$ 项。我们来看 $x^2$ 和 $y^2$ 的系数：

- $x^2$ 的系数是 $t_0^2-2$；
- $y^2$ 的系数是 $2t_0^2$，因为 $t_0\neq0$，所以这个系数恒为正。

分类讨论：

1. 当 $t_0^2>2$ 时，$x^2$ 系数为正，$y^2$ 系数也为正，两个二次项同号，所以 $M$ 是**椭圆**；
2. 当 $t_0^2=2$ 时，$x^2$ 系数为 0，方程中只含 $y^2$ 项，退化为**抛物线**；
3. 当 $0<t_0^2<2$ 时，$x^2$ 系数为负，$y^2$ 系数为正，两个二次项异号，所以 $M$ 是**双曲线**。

**【板书要点】**

- 设 $P(x,y)$，$B(0,y_0)$；
- $P\in AO$：$\dfrac{y}{x}=\dfrac{y_0}{x_0}$；
- $P\in GB$：$\dfrac{y}{x-t_0}=-\dfrac{y_0}{t_0}$；
- 解得 $x_0=\dfrac{t_0x}{t_0-x}$，$y_0=\dfrac{t_0y}{t_0-x}$；
- 轨迹方程：$(t_0^2-2)x^2+2t_0^2y^2+4t_0x-2t_0^2=0$；
- 类型：$t_0^2>2$ 椭圆；$t_0^2=2$ 抛物线；$0<t_0^2<2$ 双曲线。

**【提醒学生】**

这里最容易出错的是消参过程。大家要记住：求轨迹方程时，先用所求点的坐标表示参数点的坐标，再代入参数点满足的关系式。

---

##### 第（2）问第（ii）小题：$M$ 的中心点与 $M'$ 的形状

**【教师讲解】**

好，我们来看第二问的第二小问。这一问和第（i）小问有关系，但侧重点不同。第（i）小问是判断 $M$ 是什么曲线，而这一问要研究 $M$ 有没有中心点，以及把 $M$ 平移后得到 $M'$，$M'$ 是什么形状。

首先，什么情况下二次曲线有中心点呢？

对于不含 $xy$ 项的二次曲线 $Ax^2+Cy^2+Dx+Ey+F=0$，如果 $A$ 和 $C$ 都不为零，那么我们可以通过配方把它写成：

$$A(x-x_0)^2+C(y-y_0)^2=k$$

这时它关于点 $(x_0,y_0)$ 对称，所以 $(x_0,y_0)$ 就是它的对称中心。

如果 $A$ 和 $C$ 中有一个为零，比如 $A=0$ 而 $C\neq0$，方程就变成 $Cy^2+Dx+F=0$，这是抛物线型，它只有一条对称轴，没有对称中心。

所以，**$M$ 有中心点的条件是 $A\neq0$ 且 $C\neq0$**。

在我们这道题里，轨迹方程是：

$$(t_0^2-2)x^2+2t_0^2y^2+4t_0x-2t_0^2=0$$

所以 $A=t_0^2-2$，$C=2t_0^2$。

因为 $t_0\neq0$，所以 $C=2t_0^2>0$，恒不为零。

因此，$M$ 是否有中心，只取决于 $A=t_0^2-2$ 是否为零：

- 当 $t_0^2=2$，即 $t_0=\pm\sqrt{2}$ 时，$A=0$，方程退化为抛物线，$M$ 没有中心点；
- 当 $t_0^2\neq2$ 时，$A\neq0$，$M$ 有中心点。

这就是第一小问的答案：当 $t_0^2\neq2$ 时，$M$ 有中心点。

接下来，当 $M$ 有中心点时，我们要把 $M$ 平移到 $M'$，使原点 $O$ 成为 $M'$ 的中心点，并说明 $M'$ 的形状。

**第一步：求 $M$ 的中心坐标。**

我们用配方法。把方程按 $x$ 配方：

$$(t_0^2-2)x^2+4t_0x+2t_0^2y^2-2t_0^2=0$$

把含 $x$ 的项提取 $(t_0^2-2)$：

$$(t_0^2-2)\left[x^2+\frac{4t_0}{t_0^2-2}x\right]+2t_0^2y^2-2t_0^2=0$$

配方：

$$(t_0^2-2)\left[x+\frac{2t_0}{t_0^2-2}\right]^2-(t_0^2-2)\cdot\frac{4t_0^2}{(t_0^2-2)^2}+2t_0^2y^2-2t_0^2=0$$

整理常数项：

$$(t_0^2-2)\left[x+\frac{2t_0}{t_0^2-2}\right]^2+2t_0^2y^2=2t_0^2+\frac{4t_0^2}{t_0^2-2}$$

右边通分：

$$2t_0^2+\frac{4t_0^2}{t_0^2-2}=\frac{2t_0^2(t_0^2-2)+4t_0^2}{t_0^2-2}=\frac{2t_0^4-4t_0^2+4t_0^2}{t_0^2-2}=\frac{2t_0^4}{t_0^2-2}$$

所以配方结果为：

$$(t_0^2-2)\left(x+\frac{2t_0}{t_0^2-2}\right)^2+2t_0^2y^2=\frac{2t_0^4}{t_0^2-2}$$

从这个式子可以直接读出 $M$ 的中心坐标为：

$$\left(-\frac{2t_0}{t_0^2-2},\;0\right)$$

**第二步：平移使中心到原点。**

我们要把 $M$ 平移，使中心移到原点。设平移后的坐标为 $(x',y')$，则：

$$x'=x+\frac{2t_0}{t_0^2-2},\quad y'=y$$

也就是：

$$x=x'-\frac{2t_0}{t_0^2-2},\quad y=y'$$

代入配方后的方程：

$$(t_0^2-2)x'^2+2t_0^2y'^2=\frac{2t_0^4}{t_0^2-2}$$

两边同除以 $\dfrac{2t_0^4}{t_0^2-2}$，得到标准形式：

$$\frac{(t_0^2-2)^2}{2t_0^4}x'^2+\frac{t_0^2-2}{t_0^2}y'^2=1$$

这就是 $M'$ 的方程。

**第三步：说明 $M'$ 的形状。**

现在看 $x'^2$ 和 $y'^2$ 的系数：

- $x'^2$ 的系数是 $\dfrac{(t_0^2-2)^2}{2t_0^4}$，因为 $t_0\neq0$，这个系数恒为正；
- $y'^2$ 的系数是 $\dfrac{t_0^2-2}{t_0^2}$，正负取决于 $t_0^2-2$ 的符号。

分类讨论：

1. 当 $t_0^2>2$ 时，$y'^2$ 系数为正，两个平方项系数同为正，所以 $M'$ 是**椭圆**；
2. 当 $0<t_0^2<2$ 时，$y'^2$ 系数为负，两个平方项系数一正一负，所以 $M'$ 是**双曲线**。

注意，当 $t_0^2=2$ 时，$M$ 没有中心点，所以不存在这样的 $M'$。

**【板书要点】**

- $M$ 有中心 $\iff A=t_0^2-2\neq0$，即 $t_0^2\neq2$；
- 配方得中心：$\left(-\dfrac{2t_0}{t_0^2-2},\;0\right)$；
- 平移：$x'=x+\dfrac{2t_0}{t_0^2-2}$，$y'=y$；
- $M'$ 方程：$\dfrac{(t_0^2-2)^2}{2t_0^4}x'^2+\dfrac{t_0^2-2}{t_0^2}y'^2=1$；
- $M'$ 形状：$t_0^2>2$ 椭圆；$0<t_0^2<2$ 双曲线。

**【提醒学生】**

有些同学可能会问：第二问的结果和第一问不是一样吗？确实，$M'$ 的形状和 $M$ 的类型是一致的，因为平移不改变曲线的形状。但是第二问要求你显式地完成"配方求中心—平移得标准方程"这一过程，这是本题想要考查的核心能力。

另外，求中心点时要用**配方法**，不要用偏导数。偏导数是大学微积分的内容，高中解析几何中判断二次曲线中心的标准方法就是配方。

---

##### 课堂小结

好，我们总结一下这道题。

**第一问**考查椭圆通径。大家要记住：过焦点且垂直于长轴的弦叫通径，其长度为 $\dfrac{2b^2}{a}$。

**第二问第（i）小题**考查轨迹方程。关键步骤是：设出动点坐标，利用几何关系把参数点坐标用动点坐标表示，再代入参数点满足的条件消参。

**第二问第（ii）小题**考查二次曲线的中心对称性。不含 $xy$ 项的二次曲线 $Ax^2+Cy^2+\cdots=0$ 有对称中心的条件是 $A$ 和 $C$ 都不为零。通过配方可以找到中心，再通过坐标平移得到标准方程。

最后提醒大家：轨迹问题一定要注意变量的取值范围，比如本题中 $y_0\neq0$、$t_0\neq0$，这些条件会影响轨迹是否包含某些特殊点。在高考中，轨迹方程的"完备性"和"纯粹性"是常被扣分的地方。

今天的讲解就到这里，大家课后把这道题完整做一遍，特别注意消参和配方两个环节。
